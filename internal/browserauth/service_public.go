// Package browserauth is reserved for optional browser-login adapters.
//
// The DS2API core keeps only opaque profile identifiers. Provider-specific
// browser automation lives outside the core so deployments can choose their
// own authorized login runtime without coupling account routing to one vendor.
package browserauth
